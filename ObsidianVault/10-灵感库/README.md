# 灵感库（已并入 ObsidianVault）

承接原 `D:/workbuddy/灵感库`，现为本 Obsidian 仓库的 `10-灵感库/` 子区。

## 内容
- `cards/` — 所有素材分析卡片（每张带 YAML frontmatter，供 Bases 查询）
- `profile/` — 用户画像（我的情况.md，每次处理前必读）
- `收件箱/` — 社媒导出文件丢这里，说"处理收件箱"自动提炼
- `index.html` + `data.js` — 浏览器可视化看板（双击 index.html）
- `灵感库总览.base` — Obsidian Bases 总览（表格/卡片视图）

## 强强联合规则
- 灵感库 = 采集+深加工引擎（由 WorkBuddy + inspiration-vault skill 自动维护）
- Obsidian = 关联+检索+图谱大脑（双向链接、Bases 查询、Graph 视图）
- 卡片间、卡片与 `20-国考申论/高分知识库/` 之间用 `[[wikilinks]]` 连通
- 新卡片必须带 frontmatter（type/title/platform/url/added/tags/rating/depth）
